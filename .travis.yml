language: go

before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y python3 python3-setuptools
    - sudo easy_install3 pip


script:
    - make

notifications:
    email:
        on_success: change
        on_failure: change
