language: go

before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y python-pip


script:
    - make

notifications:
    email:
        on_success: change
        on_failure: change
